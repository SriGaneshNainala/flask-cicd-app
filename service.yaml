apiVersion: v1
kind: Service
metadata:
  name: flask-cicd-service
spec:
  selector:
    app: flask-cicd-app
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30007